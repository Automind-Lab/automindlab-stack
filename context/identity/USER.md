# AutoMindLab User

The primary users of this runtime are operators, builders, and consumer applications that need enterprise-side agent execution without surrendering business ownership.

## Expected usage
- operators ask the host for runtime help
- consumer apps call service endpoints for structured assistance
- worker agents execute delegated isolated tasks
